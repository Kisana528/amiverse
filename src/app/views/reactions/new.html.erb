<%= form_with(model: @reaction) do |form| %>
  <% if @reaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@reaction.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% @reaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :reaction_type, style: "display: block" %>
    <%= form.text_field :reaction_type %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :category, style: "display: block" %>
    <%= form.text_field :category %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
